{관리자모드 접속} -계정생성
cd c:/sqlplus sys/123456 as sysdba 
(일반접속 계정 생성)
SQL>  create user c##java identified by 1234;
(계정의 권한 부여)
SQL> grant connect,resource,dba to c##java;
SQL> exit;
[접속]
cd c:/
sqlplus
사용자아이디 : c##java
암호:1234 ->입력값은 보이지 않음 (비공개처리)

SQL>

slecet (출력) from dual(가상테이블)

실습 1 
테이블 
(1) 테이블 목록
selet tname from tab;
(2)테이블 생성
create table test1 (a varchar2(5),b varchar2(5))    -> varchar 문자타입;
